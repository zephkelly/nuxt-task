// Auto-imported from server/utils; the reminders task calls sendAlert() with
// no explicit import statement.
export function sendAlert(message: string) {
    console.log(`[alert] ${message}`)
    return true
}
